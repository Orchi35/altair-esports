// Add approved new-season portraits here, keyed by lowercase in-game name.
// Old roster image fields intentionally do not populate this collection.
export const PLAYER_PORTRAITS = Object.freeze({});
